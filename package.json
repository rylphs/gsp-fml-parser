{
  "name": "formula-parser",
  "version": "1.0.0",
  "description": "Google spreadsheet formula parser for writing custom formulas.",
  "main": "index.js",
  "scripts": {
    "build-formula-grammar": "nearleyc ./formula-grammar.ne --out ./formula-grammar.js",
    "build-args-grammar": "nearleyc ./argument-grammar.ne --out ./argument-grammar.js",
    "build-parser": "tsc --target ES5 formula-parser.ts",
    "build": "npm run build-formula-grammar && npm run build-args-grammar && npm run build-parser",
    "test": "npm run build && node test.js",
    "test-grammar": "nearleyc ./formula-grammar.ne --out ./formula-grammar.js && nearley-test ./formula-grammar.js -i $1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rylphs/gsp-fml-parser.git"
  },
  "keywords": [
    "spreadsheet",
    "google",
    "formula"
  ],
  "author": "Raphael Couto Ferreira",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/rylphs/gsp-fml-parser/issues"
  },
  "homepage": "https://github.com/rylphs/gsp-fml-parser#readme",
  "dependencies": {
    "moo": "^0.4.3",
    "nearley": "^2.11.0"
  },
  "devDependencies": {
    "typescript": "^2.5.3"
  }
}
